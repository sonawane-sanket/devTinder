const JWT_SECRET_KEY = "DEV@TINDER$";

const STATUS = {
  IGNORED: "ignored",
  INTERESTED: "interested",
  ACCEPTED: "accepted",
  REJECTED: "rejected",
};

module.exports = {
  JWT_SECRET_KEY,
  STATUS,
};
